<Window x:Class="PixelViwer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PixelViwer"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        AllowDrop="True"
        Drop="Window_Drop"
        DragEnter="Window_DragEnter"
        d:DataContext="{d:DesignInstance Type=local:MainUI, IsDesignTimeCreatable=True}"
        Title="MainWindow" Width="600" Height="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition  Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Image Source="{Binding Bmp}"/>
            <!--<Path Stretch="Uniform" Stroke="Red" Data="M9,16V10H5L12,3L19,10H15V16H9M5,20V18H19V20H5Z"/>-->
        </Grid>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <TextBlock Text="Width:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Width}" Width="60"/>
            <TextBlock Text="Height:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Height}" Width="60"/>
        </StackPanel>
    </Grid>
</Window>
